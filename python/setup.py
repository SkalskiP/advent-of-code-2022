import setuptools

setuptools.setup(
    name="advent",
    version="1.0.0",
    packages=setuptools.find_packages(),
    include_package_data=True,
    install_requires=[],
    extras_require={},
    setup_requires=[],
    tests_require=[],
)
